fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9
httpx>=0.27
python-dotenv>=1.0
pydantic>=2.6

# Only required when AI_PROVIDER=vertex (Google Vertex AI). Comment out
# these two lines if you never use Vertex — saves ~5MB of install footprint.
# google-auth needs `requests` as its token-refresh transport.
google-auth>=2.28
requests>=2.31
